import React from 'react';
import * as Styles from './styles';

const ReviewForm = (props) => {
  const { style } = props;

  return <Styles.Container style={style} />;
};

export default ReviewForm;
