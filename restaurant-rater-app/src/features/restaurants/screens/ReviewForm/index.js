import React from 'react';
import { ReviewForm } from 'features/restaurants/containers';
import { ScreenContainer } from 'shared/components';

const ReviewFormScreen = () => {
  return (
    <ScreenContainer>
      <ReviewForm />
    </ScreenContainer>
  );
};

export default ReviewFormScreen;
