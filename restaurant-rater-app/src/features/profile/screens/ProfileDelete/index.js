import React from 'react';
import { ProfileDelete } from 'features/profile/containers';
import { ScreenContainer } from 'shared/components';

const ProfileDeleteScreen = () => {
  return (
    <ScreenContainer>
      <ProfileDelete />
    </ScreenContainer>
  );
};

export default ProfileDeleteScreen;
