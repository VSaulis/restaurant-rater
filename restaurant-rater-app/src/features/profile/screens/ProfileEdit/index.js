import React from 'react';
import { ProfileEdit } from 'features/profile/containers';
import { ScreenContainer } from 'shared/components';

const ProfileEditScreen = () => {
  return (
    <ScreenContainer>
      <ProfileEdit />
    </ScreenContainer>
  );
};

export default ProfileEditScreen;
